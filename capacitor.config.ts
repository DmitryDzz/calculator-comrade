import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.dmitrydzz.calculatorcomrade",
    appName: "Calculator Comrade",
    webDir: "dist",
};

export default config;
